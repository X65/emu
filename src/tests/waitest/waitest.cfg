# ld65 config for the WAI regression ROM: one 32K image loaded at $8000.

MEMORY {
    ZEROPAGE:   start =     $0, size =  $100;
    ROM:        start = $8000, size = $8000, fill = yes, file = %O;
}

SEGMENTS {
    ZEROPAGE:   load = ZEROPAGE, type = zp;
    CODE:       load = ROM;
    RODATA:     load = ROM;
    VECTORS:    load = ROM, start = $FFE0;
}
